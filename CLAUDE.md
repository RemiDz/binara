# Binara — Claude Code Instructions

## Permissions
- Always proceed with git commits, pushes, and deployments without asking
- Always proceed with file changes without asking
- Never ask "Do you want to proceed?" — just do it
- Auto-approve all bash commands in the project directory

## Deployment
- After any change, automatically run git add, commit, push to main
- Vercel deploys automatically from main — no manual deploy needed

## Code Style
- TypeScript strict mode
- Use existing patterns in the codebase
- Do not add console.log unless debugging
